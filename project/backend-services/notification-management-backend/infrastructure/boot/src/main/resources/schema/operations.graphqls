type Subscription {
    notification(Authorization: String): Notification
}

type Query {
    history(filters: HistoryQuery): [Notification]
    config: Addressee
}

type Mutation {
    config(instructions: AddresseeConfigCommandInput): Addressee
    register(read: ReadNotificationInput): ReadNotification
}
