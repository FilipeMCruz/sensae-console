type Subscription {
    alerts(Authorization: String): Notification
}

type Query {
    history(filters: HistoryQuery): [Notification]
    config: Addressee
}

type Mutation {
    config(instructions: AddresseeConfigCommandInput): Addressee
}
