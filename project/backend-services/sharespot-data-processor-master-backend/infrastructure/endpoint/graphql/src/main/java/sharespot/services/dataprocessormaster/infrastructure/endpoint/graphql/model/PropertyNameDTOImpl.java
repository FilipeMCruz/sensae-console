package sharespot.services.dataprocessormaster.infrastructure.endpoint.graphql.model;

public enum PropertyNameDTOImpl {
    DATA_ID,
    DEVICE_ID,
    DEVICE_NAME,
    DEVICE_RECORDS,
    REPORTED_AT,
    LATITUDE,
    LONGITUDE,
    TEMPERATURE
}
