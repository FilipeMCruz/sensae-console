type Query {
    authenticate: AccessToken
    viewDomain(top: ViewDomain): [Domain]
}

type Mutation {
    createDomain(domain: CreateDomain): Domain
    moveTenant(instructions: AddTenantToDomain): Boolean
    moveDevice(instructions: AddDeviceToDomain): Boolean
}
