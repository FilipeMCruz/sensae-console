type Subscription {
    locations : GPSSensorData
    location(devices: [String]) : GPSSensorData
    locationByContent(content: String) : GPSSensorData
}

type Query {
    history(filters: GPSSensorDataQuery) : [GPSSensorDataHistory]
    latest: [GPSSensorData]
}
